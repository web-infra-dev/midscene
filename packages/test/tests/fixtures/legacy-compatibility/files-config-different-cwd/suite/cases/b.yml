interface:
  module: ${COMPAT_DEVICE}
  param:
    root: ${COMPAT_ROOT}
    log: ${COMPAT_LOG}
agent:
  generateReport: false
  autoPrintReportMsg: false
config:
  output: ${COMPAT_ROOT}/output.json
tasks:
  - name: b
    flow:
      - javascript: "record('b')"
