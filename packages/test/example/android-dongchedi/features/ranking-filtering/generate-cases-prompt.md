# 生成排行榜筛选测试

帮我根据 [产品需求](product-requirements.md) 使用 Midscene Test Runner 写法，写一下排行榜筛选的测试 Case。

生成时不要读取 `app-context/` 目录；Context 只在 Case 执行时使用。

请根据产品需求中的 5 个验收场景，在 `test-runner/ranking-filtering.yaml` 中生成 5 个可以独立运行的 Test Runner Case。

写完后检查 YAML 中的 Node 和变量是否来自当前 Test Project，再简单告诉我生成了哪些 Case。
