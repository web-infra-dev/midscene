tasks:
  - name: Verify search tab isolation and setup state
    flow:
      - javascript: verifyParallelYamlCase('search')
      - javascript: openParallelYamlPopup('search')
      # Allow popup adoption without introducing screenshot work into this
      # concurrent browser-state regression fixture.
      - javascript: new Promise((resolve) => setTimeout(resolve, 500))
      - javascript: verifyParallelYamlPopup('search')
