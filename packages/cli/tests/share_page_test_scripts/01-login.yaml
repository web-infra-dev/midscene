web:
  url: ${SHARED_BROWSER_TEST_ORIGIN}/share-context-test.html

tasks:
  - name: Set setup page state
    flow:
      - javascript: |
          sessionStorage.setItem('sessionId', 'setup-session');
          window.name = 'setup-page';
