/// <reference path="../../../../src/ts-runner/global.d.ts" />

// Top-level await style script (no exports)
// This script would normally have await agent.launch() or await agent.connect()
// but for testing we just export nothing

// Simulate some async operation
await Promise.resolve();
