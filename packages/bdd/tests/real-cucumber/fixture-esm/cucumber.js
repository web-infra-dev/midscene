// The ESM analog of ../fixture/cucumber.js — this file IS the README's
// "ESM project" cucumber config snippet, so the snippet stays proven.
import { defineProfile } from '@midscene/bdd/profile';

export default defineProfile().default;
