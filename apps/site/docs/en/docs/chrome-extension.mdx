# Experience with Chrome extension

By using the Midscene.js Chrome extension, you can experience the main features of Midscene immediately on any webpage, and you won’t need to set up a code-based project.


## Preparation

Prepare an OpenAI API key, we will use it soon.


## Install and Config

Install Midscene extension from chrome extension store: 


Start the extension (may be folded by default), setup the config by pasting the config in the K=V format:

```shell
OPENAI_API_KEY="sk-replay-your-all"

# this is optional
OPENAI_BASE_URL="https://..."
```

## Launch

You can experience the extension now ! 
