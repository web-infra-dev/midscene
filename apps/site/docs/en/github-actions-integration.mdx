# Integrate Midscene with GitHub Actions

This guide shows you how to add Midscene checks for Web, headless Linux, Windows, macOS, Android, and iOS to GitHub Actions. Use the midscene-example workflows as templates, configure GitHub repository secrets and variables, then collect HTML reports and diagnostics as GitHub artifacts.

In this guide, GitHub CI means GitHub Actions. Its workflow files, GitHub-hosted runners, secrets, variables, artifacts, and `gh` commands do not apply directly to another CI provider.

## Add Midscene to a GitHub repository

Start with one platform workflow, then adapt its install and test commands to your repository. The linked workflows live in [midscene-example](https://github.com/web-infra-dev/midscene-example), so they stay focused on application integration instead of Midscene's own monorepo build.

1. Copy the platform workflow into `.github/workflows/` and keep its `pull_request` and `workflow_dispatch` triggers.
2. Add `MIDSCENE_MODEL_API_KEY` as a GitHub Actions repository secret.
3. Add `MIDSCENE_MODEL_BASE_URL`, `MIDSCENE_MODEL_NAME`, and `MIDSCENE_MODEL_FAMILY` as GitHub Actions repository variables.
4. Update `pull_request.paths` to include the platform package and every shared package that can affect it.
5. Keep the `actions/upload-artifact` steps so successful and failed jobs both retain reports and diagnostics.
6. Push the branch, open the **Actions** tab, and inspect the platform job and its artifacts.

GitHub does not pass repository Actions secrets to workflows triggered from untrusted forks. Run model-backed checks from a trusted branch or use a reviewed relay branch in the main repository.

## Choose a GitHub Actions workflow

Each example workflow exercises a platform adapter against a GitHub-hosted desktop, simulator, emulator, or browser.

| Platform | Workflow and GitHub-hosted runner | Interface target | Validation |
| --- | --- | --- | --- |
| Web | [Playwright Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/playwright-demo.yaml), `ubuntu-latest` | Desktop Chrome through Playwright | TodoMVC actions, queries, and report generation |
| Headless Linux | [Computer Electron Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/computer-electron-demo.yaml), `ubuntu-22.04` | Electron app on an X virtual framebuffer (Xvfb) | Desktop automation without a physical display |
| Windows | [Windows Computer Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/windows-computer-demo.yaml), `windows-2022` | Chrome TodoMVC on a GitHub-hosted Windows desktop | Native desktop input, screenshots, and TodoMVC |
| macOS | [macOS Computer Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/macos-computer-demo.yaml), `macos-15-intel` | Safari TodoMVC on a GitHub-hosted macOS desktop | Native desktop input, screenshots, and TodoMVC |
| Android | [Android Emulator Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/android-emulator-demo.yaml), `ubuntu-22.04` | Pixel 6 emulator with Android API 35 | Android Debug Bridge input, screenshots, and TodoMVC |
| iOS | [iOS Simulator Demo](https://github.com/web-infra-dev/midscene-example/blob/main/.github/workflows/ios-simulator-demo.yaml), `macos-15` | iPhone Simulator, Mobile Safari, and WebDriverAgent | Simulator input, screenshots, and TodoMVC |
| HarmonyOS | No GitHub Actions workflow | Not covered | Validate on a HarmonyOS device or device farm |

Each demo job runs the target example and uploads the Midscene report so you can inspect the completed actions.

## Verify the GitHub Actions integration

These successful GitHub Actions runs show the expected result for each platform. Open the archived TodoMVC report to inspect the completed Midscene actions.

| Platform | Successful GitHub Actions run | Archived TodoMVC report |
| --- | --- | --- |
| Web | [Run 29302040780](https://github.com/web-infra-dev/midscene/actions/runs/29302040780) | [Open the Web report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-web.html) |
| Headless Linux | [Run 29252028039](https://github.com/web-infra-dev/midscene/actions/runs/29252028039) | [Open the Headless Linux report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-linux.html) |
| Windows | [Run 29800318468](https://github.com/web-infra-dev/midscene/actions/runs/29800318468) | [Open the Windows report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-windows.html) |
| macOS | [Run 29822678312](https://github.com/web-infra-dev/midscene/actions/runs/29822678312) | [Open the macOS report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-macos.html) |
| Android | [Run 29822678280](https://github.com/web-infra-dev/midscene/actions/runs/29822678280) | [Open the Android report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-android.html) |
| iOS | [Run 29822678283](https://github.com/web-infra-dev/midscene/actions/runs/29822678283) | [Open the iOS report](https://lf3-static.bytednsdoc.com/obj/eden-cn/nupipfups/Midscene/github-ci/todo-mvc-ios.html) |

:::warning
Desktop and mobile GitHub artifacts expire after seven days and may require sign-in; use the archived reports above for long-term access.
:::

## Upload reports and diagnostics as GitHub artifacts

The platform workflows upload evidence even when a job fails. Use the HTML report to replay Midscene actions, then use the diagnostics artifact to inspect the runner or device state.

| Platform | Smoke report | TodoMVC report | Diagnostics |
| --- | --- | --- | --- |
| Windows | `windows-desktop-smoke-report` | `windows-todo-mvc-report` | `windows-desktop-diagnostics` |
| macOS | `macos-desktop-smoke-report` | `macos-todo-mvc-report` | `macos-desktop-diagnostics` |
| Android | `android-emulator-smoke-report` | `android-todo-mvc-report` | `android-emulator-diagnostics` |
| iOS | `ios-simulator-smoke-report` | `ios-todo-mvc-report` | `ios-simulator-diagnostics` |
| Headless Linux | `headless-linux-ai-report` | Included in the AI report | Workflow logs |
| Web | `e2e-report` | Included in the end-to-end report | `e2e-cache-report` and `e2e-report-output` |

Diagnostics can contain runner metadata, screenshots, platform logs, package manifests, and step outcomes. The exact files depend on the platform workflow.

## Understand the GitHub-hosted runner boundary

These workflows prove the tested paths on specific GitHub-hosted runners. They do not certify every device, display, permission, or remote-session configuration.

- **Windows**: Uses an interactive session at 96 dots per inch (DPI), which is 100% scaling. It does not cover elevated windows, mixed DPI, or Remote Desktop Protocol (RDP) sessions
- **macOS**: Uses the hosted interactive desktop with Accessibility enabled. It does not cover every macOS version or permission policy
- **Android**: Uses an x86_64 Pixel 6 emulator with Android API 35. It does not replace physical-device testing
- **iOS**: Uses an iPhone Simulator and Mobile Safari through WebDriverAgent. It does not replace physical-device testing
- **Headless Linux**: Uses Xvfb instead of a physical display. Window manager and graphics-driver behavior can differ from a workstation
- **Web**: Uses Desktop Chrome through Playwright. Other browsers require separate validation
- **HarmonyOS**: Has no GitHub Actions smoke. Validate the target device before shipping
