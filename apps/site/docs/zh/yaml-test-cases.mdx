# 编写和运行 YAML 测试用例

框架维护者完成 Test Project 配置后，人类或 Agent 可以使用项目注册的 Node 编写 YAML 测试用例。开始编写前，建议运行 `describe-nodes` 生成 Node Reference，确认项目提供的能力和输入参数。

如果你尚不了解整体设计，请先阅读 [YAML 测试运行器概览](./yaml-test-runner-overview)。如果项目尚未配置，请阅读[扩展和维护 YAML 测试运行器](./extend-yaml-test-runner)。

## 核心概念

`@midscene/test` 使用以下概念描述测试项目。

```text
Test Project 配置
└── Execution Project（Web / Android / iOS / computer）
    └── Workflow Document
        ├── Lifecycle Step
        └── Case
            └── Step
                └── Node
```

- **Node（节点）**：平台开发者注册的执行能力，例如 `aiAssert` 或 `order.create`。
- **Step（步骤）**：在 YAML 中对 Node 的一次调用。
- **Case（用例）**：由多个 Step 组成的具名测试用例。
- **Workflow Document（工作流文档）**：包含生命周期和多个 Case 的 YAML 文件。
- **Execution Project**：一次运行目标及其平台、文件和标签筛选、变量、repeat 与 retry 策略。
- **Test Project 配置**：共享 Node，以及一个或多个 Execution Project 和对应的 Workflow Document。

Node 定义团队可用的执行能力，YAML 定义每个测试用例如何组合这些能力。

## 编写 YAML 测试用例

每个 Workflow Document 必须包含非空的 `cases` 数组。每个 Case 必须包含 `name` 和非空的 `steps`。

```yaml
cases:
  - name: 创建订单
    steps:
      - order.create:
          sku: midscene-mug
          quantity: 1
      - aiAssert: 页面显示“下单成功”

  - name: 取消订单
    steps:
      - order.cancel:
          orderId: example-order-id
      - aiAssert: 页面显示“订单已取消”
```

运行器按照 YAML 中的声明顺序执行 Case。一个 Case 失败后，运行器会记录失败结果，并继续执行后续 Case。

### 编写 Step

每个 Step 只能调用一个 Node。如果调用时只需传入 `prompt` 参数，可以使用字符串简写。

```yaml
steps:
  - aiAct: 点击提交订单按钮
```

上面的写法等价于：

```yaml
steps:
  - aiAct:
      prompt: 点击提交订单按钮
```

业务 Node 可以接收自定义参数。

```yaml
steps:
  - order.create:
      sku: midscene-mug
      quantity: 2
```

### 设置超时和错误处理

`$` 用于设置由运行器控制的 Step 参数。运行器不会将这些参数传入 Node 的 `input`。

```yaml
steps:
  - order.create:
      sku: midscene-mug
      $:
        timeout: 30000
        continue-on-error: true
```

支持以下两个字段：

- `timeout`：Step 的超时时间，单位为毫秒。
- `continue-on-error`：设为 `true` 后，即使 Step 失败，运行器也会继续执行当前阶段的后续 Step。默认值为 `false`。

`continue-on-error` 只控制运行器是否继续执行。只要有 Step 失败，Case 的最终状态就是 `failed`。

### 使用 Project 变量与环境变量

运行器会在执行前递归解析 Node input：

```yaml
steps:
  - launch:
      appName: ${appName}
      reinstall: ${reinstall}
  - api.createOrder:
      baseURL: ${{TEST_API_BASE_URL}}
      payload:
        count: ${orderCount}
```

- `${name}` 读取当前 Execution Project 的 `variables`；独占整个标量时保留原始 JSON 类型。
- `${{ENV_NAME}}` 读取环境变量，结果始终是字符串。
- 对象或数组变量可以作为完整值使用，但不能嵌入更长的字符串。
- 未定义变量会在收集阶段失败。变量只解析 Node input，不解析 `$`。

Workflow YAML 不提供 `set`、`saveAs` 或 Step 输出表达式。自然语言 Node 可以通过运行器维护的只读执行历史理解前序结果。

### 使用 tags 筛选 Case

```yaml
cases:
  - name: Android 冒烟下单
    tags: [smoke, android]
    steps:
      - aiAct: 完成下单
```

框架维护者在每个 Execution Project 中配置 `tags.include` 与 `tags.exclude`。exclude 始终优先；include 非空时，Case 命中任意一个 include tag 即会被选中。

## 定义执行生命周期

Workflow Document 可以在 `cases` 前后声明生命周期 Step。

```yaml
beforeAll:
  - data.prepare: 创建本文件需要的测试数据

beforeEach:
  - browser.reset: 将页面恢复到初始状态

cases:
  - name: 创建订单
    steps:
      - aiAct: 创建一个订单
      - aiAssert: 页面显示“下单成功”

  - name: 取消订单
    steps:
      - aiAct: 取消最新订单
      - aiAssert: 页面显示“订单已取消”

afterEach:
  - report.save: 保存当前用例的执行信息

afterAll:
  - data.cleanup: 删除本文件创建的测试数据
```

一个 Execution Project 的完整执行顺序如下。

```text
Project setup
  Repeat 1
    setupDocument
    beforeAll
      Case 1 attempt 1：beforeEach → steps → afterEach
      Case 1 retry：    beforeEach → steps → afterEach
      Case 2 attempt 1：beforeEach → steps → afterEach
    afterAll
    文档清理阶段
  Repeat 2
    ...
Project teardown
```

各阶段的职责如下：

- `setupDocument()` 与文档清理对每个 repeat 中的每个 YAML 文件执行一次。
- `beforeAll` 和 `afterAll` 在这个范围内各运行一次。
- `beforeEach` 和 `afterEach` 对每次 Case attempt 各运行一次。
- retry 会以新的 run ID、Agent scope、缓存 scope 和 Case history 重跑整个 Case，但不会重跑 `beforeAll`。

即使 Case 主体失败，`afterEach` 仍会执行。

如果 `beforeAll` 失败，运行器会将当前文件中的 Case 标记为 `not-run`，但仍会执行 `afterAll` 和清理函数。

如果 `setupDocument()` 失败，运行器会跳过 YAML 生命周期和 Case，但仍会运行已经注册的清理函数。

Project setup 在全部 repeat 之前只执行一次，Project teardown 始终按 LIFO 顺序执行。收到中断信号时，当前 Step 会被取消，但 cleanup hook 与已注册 teardown 会使用可执行清理工作的 signal。

## 运行项目

运行当前目录中的 Test Project：

```bash
pnpm exec midscene-test
```

运行指定目录中的项目：

```bash
pnpm exec midscene-test ./e2e
```

指定配置文件或结果目录：

```bash
pnpm exec midscene-test \
  --config ./config/midscene.config.ts \
  --result-dir ./test-results
```

选择一个或多个命名 Execution Project：

```bash
pnpm exec midscene-test --project android-smoke --project ios-regression
```

Project、repeat、Workflow Document、Case、attempt 与 Step 都串行执行。`projects[].retry` 会重试整个失败 Case，`projects[].repeat` 会重复执行选中的全部 Workflow Document 生命周期。`testRunner.maxConcurrency` 当前只接受 `1`。

### 查看运行结果

每次运行都会生成稳定的公开摘要和内部事实文件：

```text
midscene_run/
├── output/
│   └── summary.json
└── report/
    └── <最终 Midscene 报告>

.midscene/test-results/<run>/
├── runs/<case-id>/<run-id>.json
├── documents/<document-id>/<document-run-id>.json
└── collection-errors/<error-id>.json
```

`summary.json` 是公开消费协议，包含 Project、repeat、Case attempt、生命周期、结果文件、收集错误和最终报告索引。路径都相对 summary 文件，便于 CI 和可视化工具移动产物目录。内部 fact 文件保存完整 Step 输入、输出、耗时与规范化错误。`--result-dir` 只修改内部 fact 目录；`output.summary` 与 `output.reportDir` 在 `midscene.config.ts` 中配置。

Case 失败或未运行、Document 失败或发生收集错误时，CLI 会返回退出码 `1`。

## 当前限制

当前版本暂不支持以下能力：

- 大于 `1` 的并发执行。
- DAG、分支、循环或 Step 并行。
- Workflow Document 之间的数据依赖。
- 从远程 URL、npm 包或 Git 仓库加载 YAML。

运行器会在执行前收集并校验所有 YAML 文件。未知的顶层字段、未知 Node、空 `cases` 或无效 Step 都会产生收集错误（collection error）。运行器会立即报告这些错误，不会等到执行相应 Case 时才报告。
