/**
 * Default browser viewport for Studio Web sessions. The renderer uses the
 * same dimensions to reserve a stable preview canvas before the first
 * screencast frame arrives.
 */
export const DEFAULT_STUDIO_WEB_VIEWPORT = {
  width: 1280,
  height: 720,
} as const;
