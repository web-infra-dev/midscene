# i18n 工作状态快照

> 临时文件，未加入 git。下次会话恢复用。
> 生成时间：2026-05-11

## 分支与 PR

- 当前分支：`feat/studio-i18n`
- 上游分支：基于 `origin/main` 的 commit `36715ad6` (PR #2449 合入点)
- 落后 main：4 个 commit (cb1cf50a, bc558253, 1514b209, b72c8be4) —— 需要 rebase
- 相关 PR：
  - **#2447** `feat(studio): add i18n with English and Chinese support` —— 当前分支的主 PR，已推送至最新 commit `c1695892`
  - **#2454** `fix(studio): anchor settings popovers to their rows` —— 从此分支抽出的纯 UI 修复，分支 `fix/studio-settings-popover-anchor` (基于 origin/main)，独立可合并

## 已落地的两个 commit

```
c1695892 feat(i18n): extract shared @midscene/i18n package and wire into studio, playground-app, visualizer
91759dba feat(studio): add i18n with English and Chinese support
```

### commit 91759dba 内容
- Studio 自研 i18n 基建（LocaleProvider/useT/字典）
- 全量替换 88 处硬编码英文为 `t('...')`，覆盖：SettingsPanel、SettingsDock、Sidebar、DeviceList、MainContent、Playground/StudioPlaygroundEmptyState、ConnectingPreview、ConnectionFailedPreview、DisconnectedPreview、ShellLayout、ModelEnvConfigModal、ModelEnvConfigStatus
- AntD ConfigProvider 接通 zh_CN / en_US

### commit c1695892 内容
- 新建 `packages/i18n/`（@midscene/i18n）：LocaleProvider / useT / useLocale / lookup / deepMerge / makeApplyStoredLocale / commonEn-Zh
- Studio 的 i18n 改成调用共享包的薄包装（`apps/studio/src/renderer/i18n/LocaleProvider.tsx`），保留 fallback 不需要 Provider 包测试
- `@midscene/playground-app`：加入 @midscene/i18n 依赖；新建 `src/i18n/locales/{en,zh}.ts`（命名空间：`app`/`sessionSetup`/`scrcpy`/`preview`/`conversation`）；以下组件已接入 `useT()`：
  - PlaygroundApp.tsx
  - SessionSetupPanel.tsx
  - ScrcpyPanel.tsx（+ scrcpy-preview.ts 的 4 个 helper 接受可选 Translator）
  - PreviewRenderer.tsx
  - panels/PlaygroundConversationPanel.tsx
- `@midscene/visualizer`：加入 @midscene/i18n 依赖；新建 `src/i18n/locales/{en,zh}.ts`（命名空间：`blackboard`/`configSelector`/`contextPreview`/`envConfigReminder`/`envConfig`/`formField`/`historySelector`/`misc`/`player`/`playgroundResult`/`promptInput`/`screenshotViewer`/`serviceMode`/`universalPlayground`/`actions`），共 ~140 条字符串；**仅 `env-config-reminder` 已接入 useT()**

## 待办（visualizer 13 个组件未替换）

字典已经备好。按文件清单：

| 文件 | 大约字符串数 |
|---|---|
| `component/blackboard/index.tsx` | 3 |
| `component/config-selector/index.tsx` | 10 |
| `component/context-preview/index.tsx` | 4 |
| `component/env-config/index.tsx` | 12 |
| `component/form-field/index.tsx` | 11 |
| `component/history-selector/index.tsx` | 8 |
| `component/misc/index.tsx` | 3 |
| `component/player/index.tsx` | 12 |
| `component/playground-result/index.tsx` | 3 |
| `component/prompt-input/index.tsx` | 7 |
| `component/screenshot-viewer/index.tsx` | 13 |
| `component/service-mode-control/index.tsx` | 7 |
| `component/universal-playground/index.tsx` | 1 |

`utils/constants.tsx` 里的 `apiMetadata.title` 25 条 action 描述以及 `trackingTip` 等 tooltip 常量需要单独处理：
- 选项 A：保留常量为英文，在消费组件里改成 `t('actions.xxx')` 直接调用
- 选项 B：把常量改成函数 `getApiMetadata(t)` 返回翻译过的对象

## 关键决策记录

1. **方案 2（共享包）** —— 用户在前次会话明确选择，弃用方案 1（props 透传）
2. **fallback 行为** —— Studio 的 `useLocale` 在没 Provider 时回退到英文 lookup，避免改 35 个测试文件
3. **共享包的 fallback** —— `@midscene/i18n` 的 `useLocale` 在没 Provider 时返回 `t(key) => key`（原样返回 key）。Studio 单独覆盖。
4. **welcome 文案拆分** —— Biome 禁用 `dangerouslySetInnerHTML`，所以拆成 `titleLine1` / `titleLine2`，组件渲染 `<br />`
5. **Studio 还没合并外部字典** —— Studio 的 LocaleProvider 目前只 merge `commonEn/Zh + studioEn/Zh`。等 visualizer/playground-app 字典稳定后，要追加合并它们的 dict
6. **popover 定位修复独立成 PR #2454** —— 不混在 i18n 分支里

## 下次恢复步骤

1. `git checkout feat/studio-i18n && git pull`
2. 检查 PR #2454 是否合并：合并了的话 `git rebase origin/main` 解掉 SettingsPanel popover 那段冲突（接受 main 的版本）
3. 继续 visualizer 13 个组件的 useT() 替换（参考 `packages/visualizer/src/i18n/locales/en.ts` 已有的 key）
4. 替换完后，回到 `apps/studio/src/renderer/i18n/LocaleProvider.tsx`，把 `playgroundAppEn/Zh` 和 `visualizerEn/Zh` 合并到 dictionaries：
   ```ts
   import { playgroundAppEn, playgroundAppZh } from '@midscene/playground-app';
   import { visualizerEn, visualizerZh } from '@midscene/visualizer';
   const dictionaries = {
     en: deepMerge(deepMerge({ common: commonEn }, visualizerEn), deepMerge(playgroundAppEn, studioEn)),
     zh: ...
   };
   ```
   注意命名空间不冲突。studio 的 `playground` 命名空间和 playground-app 的 `app/conversation/...` 不同。
5. 校验：`pnpm run lint` + `npx tsc --noEmit -p apps/studio` + `npx nx test studio`
6. 强推：`git push --force-with-lease origin feat/studio-i18n`

## 当前测试/lint 状态

- `pnpm run lint` 通过（1 个 packages/core 预存 warning，与本改动无关）
- `npx nx test studio` 35 文件 / 199 测试全部通过
- `npx nx build i18n` 通过
- `npx tsc --noEmit -p apps/studio` 通过

## 任务列表快照

```
#1 [completed] Add @midscene/i18n to playground-app
#2 [in_progress] Add @midscene/i18n to visualizer
#3 [pending] Wire all dictionaries into Studio LocaleProvider
#4 [pending] Validate build, lint, tests
#5 [completed] Extract popover positioning fix into separate PR
```
