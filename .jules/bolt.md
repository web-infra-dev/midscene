## 2024-05-18 - [Optimize DOM query in WaterFlow animation] \n **Learning:** Caching DOM elements in objects that perform repeated DOM interactions within `requestAnimationFrame` loops can reduce performance bottlenecks, but we must protect against detached elements by checking `document.body.contains(element)` if the host page dynamically updates DOM. \n **Action:** Implemented caching logic for `document.querySelector` inside `water-flow.ts` with checks to fallback gracefully and properly nullify references when elements are removed or become detached.
